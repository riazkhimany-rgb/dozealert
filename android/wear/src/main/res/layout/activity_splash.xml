<?xml version="1.0" encoding="utf-8"?>
<!--
  Wear WO-V15 branded launch: exact 48x48dp launcher-matching icon on black.
  Dedicated layout (not SplashScreen API) so Play automation always sees the icon.
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black">

    <ImageView
        android:id="@+id/splash_icon"
        android:layout_width="@dimen/splash_screen_icon_size"
        android:layout_height="@dimen/splash_screen_icon_size"
        android:layout_gravity="center"
        android:adjustViewBounds="true"
        android:contentDescription="@null"
        android:importantForAccessibility="no"
        android:scaleType="fitCenter"
        android:src="@drawable/ic_splash_icon" />
</FrameLayout>
