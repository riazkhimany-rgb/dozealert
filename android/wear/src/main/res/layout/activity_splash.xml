<?xml version="1.0" encoding="utf-8"?>
<!--
  Wear WO-V15 branded launch: 48x48dp launcher icon on black.
  Dedicated layout (not only the SplashScreen API) so Play automation always
  sees the icon. Uses @mipmap/ic_launcher so the icon is the launcher icon by
  construction rather than a hand-matched copy.
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black">

    <ImageView
        android:id="@+id/splash_icon"
        android:layout_width="@dimen/splash_screen_icon_size"
        android:layout_height="@dimen/splash_screen_icon_size"
        android:layout_gravity="center"
        android:contentDescription="@null"
        android:importantForAccessibility="no"
        android:scaleType="fitCenter"
        android:src="@mipmap/ic_launcher" />
</FrameLayout>
